#include <stdio.h>

/**
 *4-puts.c is our file
 *we are using puts instead of printf
 *in puts we write our line of code
 */

/**
 *main - my entry point and it has void
 *
 *return - should return (0) for well written code
 */
int main (void)
{ 
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
